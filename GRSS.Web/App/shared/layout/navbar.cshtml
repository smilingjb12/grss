<nav class="navbar navbar-default navbar-fixed-top" ng-controller="NavBarCtrl as vm">
    <div class="container">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse"
                    data-target="#navbar" aria-expanded="false" aria-controls="navbar">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" ui-sref="{{ STATES.HOME }}">
                GRSS
            </a>
        </div>
        <div id="navbar" class="navbar-collapse collapse">
            <ul ng-if="!vm.user.isAuthenticated" class="nav navbar-nav navbar-right">
                <li><a ui-sref="{{ STATES.LOGIN }}">LOG IN</a></li>
                <li><a ui-sref="{{ STATES.REGISTER }}">REGISTER</a></li>
            </ul>
            <ul ng-if="vm.user.isAuthenticated" class="nav navbar-nav navbar-right">
                <li class="dropdown">
                    <a class="dropdown-toggle" data-toggle="dropdown" role="button"
                       aria-haspopup="true" aria-expanded="false">
                        {{ vm.user.username }}
                        <span class="caret"></span>
                    </a>
                    <ul class="dropdown-menu">
                        <li>
                            <a ng-click="vm.logout()">LOG OUT</a>
                        </li>
                    </ul>
                </li>
            </ul>
        </div>
    </div>
</nav>